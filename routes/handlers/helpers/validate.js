"use strict";

const name = (input) => {};

const tempo = (input) => {};

const timeSignature = (input) => {};

const message = (input) => {};

const setting = (input) => {};

const type = (input) => {
  values = [
    "audio/aac",
    "audio/mpeg",
    "audio/ogg",
    "audio/webm",
    "audio/wave",
    "audio/midi",
  ];
};

module.exports = {
  name,
  tempo,
  timeSignature,
  message,
  setting,
  type,
};
